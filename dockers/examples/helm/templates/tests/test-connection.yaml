apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "teable.fullname" . }}-test-connection"
  labels:
    {{- include "teable.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "teable.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
