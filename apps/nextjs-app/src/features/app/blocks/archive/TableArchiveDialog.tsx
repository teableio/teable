export interface ITableArchiveDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
  tableId: string;
}

export const TableArchiveDialog = (_props: ITableArchiveDialogProps) => null;
