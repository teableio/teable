import { IViewSnapshot, OpBuilder, ViewCore } from '@teable-group/core';
import { Doc } from 'sharedb/lib/client';
import { IFieldInstance } from './factory';

export class View extends ViewCore {
  async updateViewName(name: string) {}
}
